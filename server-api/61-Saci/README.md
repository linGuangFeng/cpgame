# 61 Saci Controller v3

平台注入 `--port`（50000–59999）。唯一结果源是 Redis 预生成完整局。

```
java -jar dist/controller.jar --port 51061 --config dist/controller.properties --publish ..\..\publish\61-Saci
```

试玩：见 `demo-url.txt`。
