# Sharpshooter contract v3 Controller

The platform supplies a dynamic 50000-59999 port, for example `java -jar dist/controller.jar --config dist/demo-controller.properties --port 51090`. Spin responses are projected only from one claimed pre-generated Redis member; cache exhaustion is HTTP 503 and never falls back to fixtures or runtime generation.
