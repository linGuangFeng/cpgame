# 1810 Treasure Hunt local controller

Managed-process Java 17 service for the preserved publish package. Every paid spin is selected from independently generated Redis db15 caches; it exposes the provider init/spin/history paths and local QA/history paths.
