# 1670 Christmas Gift local controller

Managed-process Controller Contract v3 implementation. It binds one dynamic 5xxxx port on `0.0.0.0`, serves the accepted original static package without modifying it, and consumes complete minimal round facts from Redis database 15. Runtime generation and fixture access are intentionally absent.

The controller persists balance, last complete result, idempotency results, QA cache selectors, and order history. A restart therefore cannot charge or settle an acknowledged request twice.
