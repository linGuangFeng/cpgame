# Beach Fun Controller v3

Run `restart-demo.cmd` with JDK 17+. The configured port is 51940 and may be overridden by a positional 5-digit port or `--port`. Results are exclusively claimed from pre-generated Redis DB15 BF1 members; an empty selected outcome fails with HTTP 503.
