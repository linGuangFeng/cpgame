
五、完成复刻实现

最终服务端 API、完整局生成器、规则核心、ResultUtil 和 Redis Loader 必须使用 Java 实现。JavaScript/Node 只能用于取证、资源处理和临时诊断。

只能维护一套 GameRuleCore，server-api、生成器和 Redis Loader 必须使用同一规则核心。

ResultUtil 必须独立复核开奖结果，测试预期必须来自原始规则、前端判定、抓包或独立 oracle，不能只证明实现与自己一致。

原站响应和 fixtures 只能用于分析与回归测试，禁止作为运行时结果源。

六、生成模型要求

分别统计初始牌面、连消补牌、重转补牌及其他真实发牌入口的符号和特殊材质分布，记录样本数、分母、约百分比和证据来源（矩阵印证）。

不得把所有格子简单地独立随机。普通自然发牌保持已验证的联合分布；专用 0 倍生成入口必须按照下述约束构造真实不中奖候选，可以对合法符号、列向量或联合状态进行规则条件采样，但不得伪造派奖字段或把中奖结算改写为 0。

特殊符号必须同时控制：

- 同一列或同一轴的列上限；
- 整盘、单页或单次旋转的总上限；
- 允许出现的轴位或格位；
- 不同补牌入口是否允许生成；
- 免费次数等派生数量上限。

规则上限与抓包上限取更严格者。抓包从未出现的数量不得生成。允许提高稀有模式权重，但不得只加权而不封顶。

使用不少于 100 个未参与建模的原厂完整局作为留出样本，并对不少于 10000 个新生成完整局验证入口分布、Round 结果、Step 数量和状态联合分布。

生成 generation-model-validation.json；核心检验未通过时继续修复。

六（一）、0 倍生成器硬性要求（2026-09-14）

适用于所有游戏，包括已人工验收通过的游戏；必须按各游戏真实中奖与特殊触发规则分别实现。

- 首次是指每次外部调用内的第一张完整候选盘，必须在任何重试和兜底之前单独统计。正常游戏首张候选必须保证 100% 不中奖，不能靠自然出牌概率、外部重试或默认池掩盖首次失败。
- 只有规则上确实难以构造的特殊游戏可以例外，须逐游戏记录原因、具体限制与实测证据；其首张不中奖率仍须达到 90% 以上。不得默认把所有游戏按 90% 标准处理。
- 每次调用在生成器内部最多尝试 5 张候选（包含第一次，共 5 次，不是首次加 5 次），每张均按真实规则复核；一旦确认 0 倍立即返回，不额外生成剩余候选。
- 5 张均未通过时，生成器内部必须从预先准备且已校验的 10 个默认不中奖结果中随机取一个返回。默认结果由合法生成模型生成，不能来自原站响应或 fixtures；按规则、配置、模式与必要的押注档区分默认池，初始化即校验，运行时不重新批量生成默认池。
- 对外只暴露一次调用即可取得真实不中奖结果的接口。不得要求调用方循环、选兜底、重新抽盘；不得在失败时返回最后一张可能中奖的候选。非法参数或损坏的配置须明确报错，不得冒充正常 0 倍结果。
- 不中奖要求完整结算实际为 0，并且不会触发免费、重转、小游戏或其他待完成奖励；仍须满足各游戏的符号位置、数量、结构及材质上限。判定不得只看被填成 LOSS 的标签或已取整的倍率。
- 并发路径必须有明确工作上限。不得在一次候选构造内隐藏整盘重试、无界随机拒绝或每请求加载模型；可预计算合法候选集合、条件权重和不可变默认池。共享缓存必须线程安全，返回对象不得污染默认池，局 ID、余额和押注字段应按当前调用重新建立。
- 验收分别记录首张候选不中奖率、外部单次调用最终不中奖率、触发/符号合法性；正常游戏至少直接检验 100000 张首张候选且零失败，并结合构造约束说明为何必然不中奖，不能把有限样本通过说成数学证明。特殊例外单独列出。
- 必须故障注入验证连续 5 次候选拒绝后使用默认池、首次/第 5 次成功时立即返回，并验证默认 10 个均不中奖、共享实例并发调用无交叉污染；报告样本量、线程数、耗时及测试环境，不能用本地微基准冒充生产 QPS。

以上兜底仅属于生成器内部，不改变第八节 Demo 只能消费 Redis 预生成完整局的限制，也不允许 Redis 为空时使用本地默认盘替代。

七、静态发布包

resources/当前ID-name 是原始归档，不是上传包。

publish/当前ID-name 才是最终可上传目录，其根目录必须直接包含原站 index.html 和 publish-manifest.json。

必须铺入已抓取的 versionconfig、cocos、settings 等原始资源。

禁止修改原前端、制作替代 Demo 页面、自绘盘面，或者依赖 _hosts、查询串目录、Java 路由和运行时注入才能找到静态文件。

使用普通静态 HTTP 服务实际验证入口和主要资源不存在 404。

八、Redis 与 Controller

Demo 结果只能从以下 Redis 读取预生成完整局：

- Host：192.168.10.3
- Port：6379
- Database：15

禁止 Demo 当场生成整局、读取 fixtures、轮播历史响应、使用本地随机替代 Redis，或者在 Redis 为空时以内存数据顶替。下述已领取 member 内的独立无奖 `#` 标记允许按规则实时物化。

每个新付费局先随机决定中奖或未中奖；Demo 目标流程为代码随机目标倍数（以对应奖池已存最大倍率为上限），使用 ZREVRANGEBYSCORE 按 [0,目标] 倒序只取最近一档，桶空则排除该档继续向下，有数据则随机下标读取一条完整局 member。41 以及本次修复的另外 23 个模块已接入，清单与包校验见 reports/demo-redis-target-floor-20260915/README.md。后续连消、免费和奖励只投影同一个 member，不得中途重新抽取。

未中奖完整局也必须提前写入 Redis；无奖占位标记也必须作为 Redis member 或 member 内 Spin 预先写入。Redis member 使用能够还原可见状态的极简 ASCII，禁止保存整份 JSON。

独立无奖标记规范（2026-09-15，1809、1407、41、1830、2350、2410、2470 已按下述范围接入）：

- 使用 ASCII `#`，不要用数字 `0`；除 41 去掉冗余头外，保留各游戏原版本头和分隔符。1809 按无奖 Spin 的可见 x2 球数量使用 `#`/`#1`/`#2`/`#3`，例如 `原编码|#2|原编码`；1407/1830 示例 `原编码|#|原编码`，普通独立无奖整局 member 可仅为 `#`。41 使用无头格式 `牌面|#N|牌面`，普通独立无奖整局可仅为 `#N`；2350 使用 `CU1PL;#`；2410 使用 `EF1:#`；2470 使用 `LNM1|L|#`，特色 F 模式内使用分号分隔的单步 `#`。
- 只能压缩与前后 Spin 无牌面继承关系的完整独立 Spin：仅一页、实际派奖为零、不触发免费/重转/奖励、不会改变后续累计倍率或其他奖励状态；41 的 `#N` 是明确例外，须完整保存 PAN 数量并精确重建倍率增量。不能只因金额为 0 或整数倍率为 0 就替换。
- 连消链、保留牌/锁定格/重转链的任何页都不得单独替换，尤其不能把连消的无奖结束页替换为 `#`。免费触发和重触发盘即使未派奖也必须保留。
- 对无牌面继承且无状态增量的免费 Spin 可用 `#`，但必须保留 Spin 的位置、次数消耗及已有累计倍率；不删除 Spin，也不重置状态。1809 的无奖倍率球增量使用 `#1`/`#2`/`#3` 精确保留，并在解码后生成对应数量的真实可见球；超过可编码数量或存在其他状态变化时保留完整牌面。其他游戏无法证明上下局无关时保留完整编码。
- 解析器只在整个 Spin token 恰为该游戏合法标记（`#` 或 41 的 `#N`）时调用当前游戏共享零倍生成器，按当前模式产生真实不中奖且不触发奖励的盘面，再由 ResultUtil 复核；不得把派奖字段硬改成零。沿用零倍生成器内部最多 5 次及 10 个已校验默认盘的约束。
- 每条 Redis member 领取后只物化一次并固定牌面；后续交付、重试、幂等与 History 复用物化结果，不能反复解码后展示不同盘面。Redis 为空仍明确失败，不能自行生成替代整局。
- 生成器必须先验证原始整局，再压缩合格 Spin；解析器、生成器和 Redis Loader 共用同一标记及判断规则。零倍按实际倍率 0 写入，索引与 List 的 ZADD + RPUSH + LTRIM 规则不变。
- 除 41 外，新解析器兼容原完整编码。41 只接受新的无头紧凑编码，旧缓存须用新 Loader 重建。先更新消费端解析器，再用新 Loader 写入带 `#` 的数据；其他游戏已有完整编码不强制迁移。验证压缩前后整局派奖、Spin 数、免费次数和累计倍率相同，并覆盖连消结束页不得压缩。

本次新增范围：

- 41：`#N` 中 N 为 PAN RLE 块数（0..29）。固定 PAN 数量和真实零奖，沿用原倍率跟踪规则：首次免费从零倍率进入时还含基础 +2，不能把 `#0` 理解成最终 rpx=0。编码前核对重建倍率与原值一致，否则保留完整盘面。普通/免费 Spin 单页、无触发/重触发才允许标记；不能替换连消页，也不与页内 `#G` / `#S` 混淆。 新缓存去掉 lp1/bs/bl/P/F 字段；固定 Map 写在共享代码中：Pan=P、H1=F、H2=G、H3=H、H4=I、H5=L、A/K/Q/J/T 原字母、Wild=W、Scat=S。高度 1 省略、2..4 保留前缀（1H3→H、2H3→2H），牌块无逗号且不合并相邻同符号块；| 分隔 Spin、; 分隔连消页，@rpx 和金银框保留。下注取实际请求，只接受新紧凑格式；不接受 lp1 头、P/F 字段或逗号分隔旧牌面，旧缓存须重新生成。
- 1830：普通/免费独立零奖 Spin 使用 `#`，普通 Scatter<3，免费 Scatter<2。触发、追加次数、连消收尾及整条连消保留。
- 2350：只接普通付费单步 LOSS (`CU1PL;#`)；扩展 Wild 免费、锁币及触发步本次不压缩。
- 2410：只接普通独立零奖整局 (`EF1:#`)；锁列重转及粘性倍率全部保留。
- 2470：普通独立零奖为 `LNM1|L|#`；Lucky Feature 仅第 2..8 步的独立零奖允许 `#`，8 步与模式头保留，特色首步和 Lucky Wheel 均保留。倍率仅作用于当前步，无需 `#N`；按 featureLater 模型生成并保留累计奖金/次数。普通零奖整局仍须普通池配置范围包含 0；特色按完整局总奖筛选。

Redis Key 必须按 betLog「结果」页的查询规则生成，禁止自造前缀、小数倍率或漏写索引。结果页只读下列索引，再按索引还原 List：

- 普通索引：`PerKeyList_%09d`（9 位游戏 ID），或 `PerKeyList_{0-3}%08d`（押注档 + 8 位游戏 ID）
- 特殊索引：`MaryKeyList_%09d`，或 `MaryKeyList_{0-3}%08d`
- 普通列表：`BetLog:0%08d:%06d`（与 `PerKeyList_%09d` 对应；倍率为整数，6 位左补 0）
- 特殊列表：`MaryLog:%09d:%06d`

写入时必须 `ZADD` 索引 + `RPUSH` 列表 + `LTRIM`。只写 List 不写 `PerKeyList`/`MaryKeyList` 时，结果页查不到。`redis.game-id` 必须与 Key 里的游戏 ID 一致。

`generator.properties` 按 1809 的分段注释写清楚：Redis 连接、游戏 ID 与 Key 规则、生成数量、批次、每倍率保留条数、连赢/免费上限、倍数范围、牌面权重。注释必须说明字段含义，禁止只写键名。

倍数默认值（出奖位置只计能中奖的格子，不含不能中奖的位置）：

- 普通奖励最小倍数：1（倍数单位默认 1）
- 特殊奖励最小倍数：5 × 中奖线数
- 普通奖励最大倍数：75 × 中奖线数
- 特殊奖励最大倍数：100 × 中奖线数
- 特殊奖励每个倍率最多保存 100 个牌面；普通奖励每个倍率默认最多 300 个
- 出奖位置小于九宫（不足 9 个能中奖格子）的游戏不限制最大倍数；最小倍数仍按上面执行
- 固定线玩法用实际中奖线数；无固定线时用押注公式里的线数/底注倍数（如 `ba = bs × bl × 20` 的 20），不得把 243/1024 Ways 当成中奖线数

Controller 使用 contract v3，交付：

- server-api/当前ID-name/dist/controller.jar
- server-api/当前ID-name/demo-controller.properties
- controller.contract-version=3
- controller.jar=dist/controller.jar

Controller 必须接受平台动态注入的 50000–59999 端口，禁止写死端口、启动额外 Node/Java 子进程或监听其他端口。

generator/当前ID-name/dist 只交付：

- 一个包含全部依赖的 Java Loader JAR；
- 一个无 seed 的 generator.properties；
- 一个可双击、自定位并支持 --no-pause 的 Windows .cmd。

正式 Loader 使用 SecureRandom。

九、真实试玩和自测

必须分别验证纯静态页面和接入 Java API 后的原页面。

至少验证：

- 所有语言和可点击入口；
- Init、Config、Spin、History、Balance、Session；
- 普通未中奖和普通中奖；
- 每种确认存在的购买、免费、特殊模式；
- Redis 为空时明确失败；
- 运行时不读取 fixtures 或历史响应；
- 每个模式至少完整触发两次。

不能用 HTTP 200、接口 JSON、进入游戏截图、Get Started 截图或停轮截图代替试玩。

必须在原页面真实点击 Spin，等待连消、特殊、免费及结算动画全部结束，确认开始按钮从置灰恢复可点击，然后再次点击并成功开启新局。

出现以下情况必须修复，不能判定通过：

- 开始按钮一直置灰；
- TOTAL WIN 卡住；
- 连消没有正确清除；
- Scatter 触发后免费流程没有走到 frees.st=0；
- 必须刷新页面才能继续 Spin；
- 某个确认模式只触发一次或只加载到一半。

完成后更新 reports/当前ID-name/current-status.json，状态设为 READY_FOR_ACCEPTANCE，并记录实际证据。

十、最终复核与交付

交付前重新读取原始规则、抓包、game-capabilities、protocol-handoff 和实现代码，自查资源遗漏、规则错误、概率失真、符号超限、状态断裂、Redis 绕过和前端试玩卡死问题。

发现问题立即修复并重新测试。只有全部核心检查通过后才能宣布完成。

最终回复必须提供：

- 已实际测试的游戏页面地址；
- 游戏 ID、名称和最终状态；
- publish 可上传目录；
- resources 原始归档目录；
- protocol-spec、protocol-handoff、game-capabilities、current-status 的路径；
- 各类别真实完整 Round 数量；
- 已覆盖语言和入口；
- History 证据；
- Controller JAR 和配置路径；
- generator、Redis Loader、generator.properties 和启动脚本路径；
- Redis 配置及 Demo 取数方式；
- 自动测试、生成模型验证和真实页面试玩结果；
- SAMPLE_INSUFFICIENT 等非阻塞缺口。




有疑问可参考已完成复刻游戏 1809

### Demo Redis 访问性能（2026-09-15）

禁止每次 Spin 全量查询所有倍率桶或逐桶同步往返校验长度。41：未中奖读取普通零倍桶；中奖随机先尝试普通/特殊池，以所选池 ZREVRANGE 0 0 返回的最大倍率为上限，代码随机 1..max，执行 ZREVRANGEBYSCORE index target 0 LIMIT 0 1。选中桶空时用排他上界 (ratio 继续向下，不选大于目标的倍率。有数据时 LLEN 后随机下标 LINDEX，仅物化一次，不消耗 demo 缓存。首选池查不到时尝试另一池，两者无可用结果才按原逻辑回退零奖或明确失败。正常命中不全扫、不需要 pipeline；已有索引和生成器无需改。验证包含大量倍率桶、空桶向下查找、随机下标、免费/连消与实际代理 spin。


### 其余 Demo 同步整改（2026-09-15）

33、42、43、50、54、56、57、58、60、61、1090、1380、1830、1940、2001007、2010、2110（含 mac 版）、2210、2290、2350、2410、2470 均改为随机目标倍率后向下取最近非空桶。共 23 个模块；已有特殊模式、投注档、倍率区间及会话状态限制继续生效。Redis 索引查询必须带 `LIMIT 0 1`；读取当前允许区间最大倍率作为随机上限，空桶使用排他上界继续向下，命中后随机 `LINDEX`，不得全量扫描倍率桶或消费 Demo 缓存。连消/免费继续使用已领取的同一完整局。

所有服务器包已打进各自 dist；2470 因生成器共享源码，同步更新 generator/2470-Lucky-Night-Market/dist/loader.jar。没有改变缓存编码、# 标记条件或上下局关联规则。详细差异、测试结果和实库空池限制见 [批量整改报告](reports/demo-redis-target-floor-20260915/README.md)。
