# Lucky Cat II (ID 50) protocol draft (superseded)

Retained only for compatibility with old indexes. The accepted specification is `protocol-spec.md`; use it together with `game-capabilities.json`, `capture-field-evidence-matrix.json`, and `protocol-handoff.json`. Authenticated wire evidence and adjacent Lucky Respin steps are now confirmed there.
