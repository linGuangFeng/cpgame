# Sharpshooter Redis Loader

`dist/run-loader.cmd --no-pause` generates complete rounds using the unique Java `GameRuleCore` and writes printable `SS1` members to Redis DB15. Entropy is internal and no fixture replay path exists.
