# Saci（61）：普通付费完整 Round 自然生成并直接写入 Redis
# 原厂权重未知；以下为当前游戏专属的本地复刻权重，不代表原厂 RTP。

# Redis 连接（凭据只在部署环境填写，Loader 不会输出凭据）
redis.host=18.234.101.161
redis.port=8021
redis.username=
redis.password=pssss@6j&NHIjpB
redis.database=0
redis.ssl=false
redis.connect-timeout-ms=5000
redis.socket-timeout-ms=30000
redis.game-id=8000061
# Redis Key 必须按 betLog「结果」页查询规则生成：
# 普通 PerKeyList_%09d + BetLog:0%08d:%06d ；特殊 MaryKeyList_%09d + MaryLog:%09d:%06d
# 倍率是整数 6 位左补 0；必须 ZADD 索引，结果页不扫自造前缀。
# 本游戏出奖位置=15（只计能中奖格子），中奖线数=20。倍数单位默认 1。
# 普通最小 1，特殊最小 100（5×中奖线数）。普通最大 1500（75×20），特殊最大 2000（100×20）。
# 普通每个倍率最多 300 个牌面；特殊每个倍率最多 100 个。

# 最多自然生成的候选数量；0 倍跳过，正倍数按实际倍率写入，最终容量由 LTRIM 控制
generation.normal-count=100000
# 免费/购买等特殊完整 Round 的相邻协议未捕获且明确禁止实现，因此必须为 0
generation.special-count=0
generation.batch-size=100
generation.max-members-per-multiplier=300
generation.special-max-members-per-multiplier=100
generation.max-consecutive-wins=10
generation.normal-max-win-multiplier=1500
generation.special-max-win-multiplier=2000

# 使用当前游戏已确认的最小押注生成倍率事实
bet.level=1
bet.size=0.02

# 当前游戏 1~8 普通符号与 9 Wild 的本地复刻权重；全部为正数且由代码实际读取
generation.symbol.1.weight=5
generation.symbol.2.weight=10
generation.symbol.3.weight=15
generation.symbol.4.weight=20
generation.symbol.5.weight=25
generation.symbol.6.weight=30
generation.symbol.7.weight=40
generation.symbol.8.weight=45
generation.symbol.9.weight=5
