# Jungle Fruit 16：完整局生成并写入 Redis。与 generator.properties 同内容，供 start-loader.cmd 读取。
# Loader 按 modes 分类写入普通/玛丽池。

game.raw-id=16
# Redis Key 使用的游戏 ID，必须与 PerKeyList/BetLog 里的 ID 一致
redis.game-id=8000016
# Redis Key 必须按 betLog「结果」页查询规则生成：
# 普通 PerKeyList_%09d + BetLog:0%08d:%06d ；特殊 MaryKeyList_%09d + MaryLog:%09d:%06d
# 倍率是整数 6 位左补 0；必须 ZADD 索引，结果页不扫自造前缀。
# 本游戏出奖位置=36（只计能中奖格子），中奖线数=20。倍数单位默认 1。
# 普通最小 1，特殊最小 100（5×中奖线数）。本 Loader 用单一 maximum-round-multiplier 同时卡住普通/特殊，取特殊上限 2000。
# 普通每个倍率最多 300 个牌面；特殊每个倍率最多 100 个。

# Redis 连接
redis.host=127.0.0.1
redis.port=6379
redis.database=15
redis.password=
redis.connect-timeout-ms=5000
redis.read-timeout-ms=10000

# LOSS 走普通池，MARY/FREE 走玛丽池
generation.modes=LOSS,MARY,FREE
generation.total-members=100000000
generation.max-members-per-multiplier=300
generation.special-max-members-per-multiplier=100
generation.max-candidates=100000000
# 完整局累计倍数上限（普通/特殊共用）
generation.maximum-round-multiplier=2000
generation.maximum-cascades=18
generation.maximum-special-spins=25

generation.bet-size=0.05
generation.bet-level=1
redis.username=
redis.ssl=false
redis.socket-timeout-ms=30000
