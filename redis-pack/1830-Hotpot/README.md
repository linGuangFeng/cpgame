# Hotpot 1830 Redis 预写入

完整局已写入 `192.168.10.3:6379` db=15，不是 JSONL 运行时源。

| 池 | Key | 已写入 |
| --- | --- | --- |
| 普通未中奖 | `BetLog:000001830:000000` 且 `PerKeyList_000001830` score=0 | 40 |
| 普通正倍 | `PerKeyList_000001830` + `BetLog:000001830:%06d` | 80 局 / 38 个整数倍率 |
| Scatter 免费（特殊/mali 池） | `MaryKeyList_000001830` + `MaryLog:000001830:%06d` | 30 局 / 29 个整数倍率 |

购买不是独立玩法，没有购买专用池。

Demo 取法：先随机中/不中，再在对应索引已有倍率中取一条 member；连消/免费只投影，禁止中途换 member。
