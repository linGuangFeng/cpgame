# raw gid 2 Redis Complete-Round 合同（未再使用）

Demo 与 dist Loader 已对齐 1400：实时 catalog，`generation.cache=false`，
`redis.enabled=false`。本目录只保留历史 key 形状说明，不再作为运行时取数合同。

原先平台 key：

- `_0` 单轴 `PerKeyList_000000002` / `BetLog:000000002:<倍率>`
- `_1` 双轴 `PerKeyList_100000002` / `BetLog:100000002:<倍率>`
