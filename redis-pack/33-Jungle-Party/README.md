# Redis contract

The generator writes one complete, independently verified gid 33 Round per atomic list member.
No captured provider response is used at runtime. The key and member codec are local contracts;
compatibility with an unknown original consumer is not claimed.
