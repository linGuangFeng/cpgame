# Redis delivery

Run `generator/1090-sharpshooter/dist/run-loader.cmd --no-pause`. The loader writes only `cpgame:v3:1090:*` keys in DB15 and does not flush the database. Controller extraction is outcome/index plus integer centi-multiplier; every continuation is projected from the same claimed SS1 member.
